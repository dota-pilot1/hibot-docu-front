# 채팅방 구현 계획

## 개요

현재 "채팅 관리" 메뉴에서 프로젝트(폴더) > 팀 구조로 되어 있으며, 팀을 선택하면 탭으로 열립니다.
여기에 **채팅방** 기능을 추가하여 팀별로 실시간 채팅이 가능하도록 구현합니다.

### 구조 변경

```
기존: 프로젝트 > 팀 (탭으로 열림 → 본문에 "구현 예정" 표시)
변경: 프로젝트 > 팀 > 채팅방 (트리 메뉴에 채팅방 추가)
```

**트리 메뉴 구조:**
```
📁 하이봇 프로젝트
  └─ 👥 백엔드 개발팀
       ├─ 💬 일반 채팅방
       ├─ 💬 기술 토론방
       └─ 💬 AI 질문방
  └─ 👥 프론트 개발팀
       └─ 💬 프론트 채팅방
📁 다른 프로젝트
  └─ ...
```

---

## 1차 구현 (기본 기능)

### 1.1 데이터베이스 설계

#### chat_rooms 테이블 (신규)
```sql
CREATE TABLE chat_rooms (
  id SERIAL PRIMARY KEY,
  team_id INTEGER NOT NULL REFERENCES chat_teams(id),
  name VARCHAR(100) NOT NULL,
  description TEXT,
  room_type VARCHAR(20) DEFAULT 'GENERAL',  -- GENERAL, AI_ENABLED
  max_participants INTEGER DEFAULT 50,
  created_by INTEGER REFERENCES users(id),
  display_order INTEGER DEFAULT 0 NOT NULL,
  is_active BOOLEAN DEFAULT TRUE NOT NULL,
  created_at TIMESTAMP DEFAULT NOW() NOT NULL,
  updated_at TIMESTAMP DEFAULT NOW() NOT NULL
);
```

#### chat_room_participants 테이블 (신규)
```sql
CREATE TABLE chat_room_participants (
  id SERIAL PRIMARY KEY,
  room_id INTEGER NOT NULL REFERENCES chat_rooms(id),
  user_id INTEGER NOT NULL REFERENCES users(id),
  joined_at TIMESTAMP DEFAULT NOW() NOT NULL,
  last_read_at TIMESTAMP,
  is_active BOOLEAN DEFAULT TRUE NOT NULL,
  UNIQUE(room_id, user_id)
);
```

#### chat_messages 테이블 (신규)
```sql
CREATE TABLE chat_messages (
  id SERIAL PRIMARY KEY,
  room_id INTEGER NOT NULL REFERENCES chat_rooms(id),
  user_id INTEGER NOT NULL REFERENCES users(id),
  message_type VARCHAR(20) DEFAULT 'CHAT',  -- CHAT, SYSTEM, AI
  content TEXT NOT NULL,
  created_at TIMESTAMP DEFAULT NOW() NOT NULL
);
```

---

### 1.2 백엔드 API

#### REST API 엔드포인트

| 메서드 | 경로 | 설명 |
|--------|------|------|
| GET | /chat-rooms/team/:teamId | 팀의 채팅방 목록 |
| GET | /chat-rooms/:id | 채팅방 상세 |
| POST | /chat-rooms | 채팅방 생성 |
| PATCH | /chat-rooms/:id | 채팅방 수정 |
| DELETE | /chat-rooms/:id | 채팅방 삭제 |
| POST | /chat-rooms/:id/join | 채팅방 참여 |
| POST | /chat-rooms/:id/leave | 채팅방 퇴장 |
| GET | /chat-rooms/:id/messages | 메시지 목록 (페이징) |

#### WebSocket 엔드포인트

| 토픽 | 용도 |
|------|------|
| /app/chat/{roomId}/message | 메시지 전송 |
| /topic/chat/{roomId} | 메시지 수신 (CHAT, SYSTEM, AI) |

---

### 1.2.1 System 메시지 설계 원칙

> **핵심 원칙: system 메시지는 "사건 알림"까지만, "상태의 정합성"은 다루지 않는다.**

#### 1차에서 해도 되는 것 ✅

**system 메시지 = 단순 알림 (stateless)**

```json
{
  "type": "SYSTEM",
  "event": "JOIN",
  "userId": 3,
  "userName": "홍길동",
  "message": "홍길동님이 입장했습니다"
}
```

```json
{
  "type": "SYSTEM",
  "event": "LEAVE",
  "userId": 3,
  "userName": "홍길동",
  "message": "홍길동님이 퇴장했습니다"
}
```

**특징:**
- 방에 들어왔다는 **사건(event)** 알림
- 채팅 타임라인에 한 줄 출력
- 서버 메모리 기반 판단 OK
- 순서 어긋나도 UX 문제 없음
- 재연결/중복/유령 유저 이슈와 무관

#### 1차에서 하지 말아야 할 것 ❌

**system 메시지로 상태 동기화 시도**

```json
// ❌ 이런 의도로 사용 금지
{
  "type": "SYSTEM",
  "onlineUsers": [1, 2, 3],
  "participantCount": 3
}
```

**금지 항목:**
- 현재 접속자 목록
- 누가 온라인인지 상태
- 참여자 수 실시간 유지
- join/leave 누적 상태 관리

👉 이런 기능은 2차에서 `/topic/chat/{roomId}/participants` 토픽으로 분리

#### 정리표

| 항목 | 1차 구현 |
|------|----------|
| system 메시지 (입장/퇴장 알림) | ⭕ |
| system으로 참여자 상태 관리 | ❌ |
| 실시간 참여자 리스트 | ❌ (2차) |
| Redis / presence | ❌ (2차) |

---

### 1.3 프론트엔드 구현

#### 트리 메뉴 수정 (ChatSidebar)

1. **팀 하위에 채팅방 목록 표시**
   - 팀 클릭 시 하위 채팅방 펼침/접힘
   - 채팅방 아이템에 💬 아이콘

2. **채팅방 CRUD**
   - 팀 컨텍스트 메뉴에 "채팅방 추가" 옵션
   - 채팅방 컨텍스트 메뉴: 이름 변경, 삭제

3. **채팅방 클릭 시 탭으로 열기**
   - 기존 탭 시스템 활용
   - 탭 타입 구분: TEAM vs ROOM

#### 채팅방 본문 (ChatRoomContent)

```
┌─────────────────────────────────────┐
│ 채팅방 헤더 (방 이름, 참여자 수)     │
├─────────────────────────────────────┤
│                                     │
│  메시지 목록 영역                    │
│  - 내 메시지: 오른쪽 정렬            │
│  - 상대 메시지: 왼쪽 정렬            │
│  - 시스템 메시지: 중앙 정렬          │
│                                     │
├─────────────────────────────────────┤
│ [메시지 입력창]            [전송]   │
└─────────────────────────────────────┘
```

#### 상태 관리 (TanStack Store 사용)

**useChatStore 확장** (`@tanstack/react-store`)

```typescript
import { Store, useStore } from "@tanstack/react-store";

export interface ChatRoom {
  id: number;
  teamId: number;
  name: string;
  description?: string;
  roomType: 'GENERAL' | 'AI_ENABLED';
  maxParticipants: number;
  createdBy?: number;
  displayOrder: number;
  isActive: boolean;
}

export interface ChatMessage {
  id: number;
  roomId: number;
  userId: number;
  userName: string;
  messageType: 'CHAT' | 'SYSTEM' | 'AI';
  content: string;
  createdAt: string;
}

export interface ChatStoreState {
  // 기존 상태 (사이드바, 패널, 탭)
  isOpen: boolean;
  sidebarWidth: number;
  expandedProjects: Set<number>;
  panels: ChatPanel[];
  activePanelId: string;

  // 채팅방 관련 추가
  expandedTeams: Set<number>;           // 펼쳐진 팀 목록
  roomsByTeam: Map<number, ChatRoom[]>; // 팀별 채팅방 캐시
  activeRoomId: number | null;          // 현재 활성 채팅방
  messagesByRoom: Map<number, ChatMessage[]>; // 방별 메시지 캐시

  // 기존 액션...
  toggle: () => void;
  setSidebarWidth: (width: number) => void;
  // ...

  // 채팅방 액션 추가
  toggleTeamExpand: (teamId: number) => void;
  setRoomsForTeam: (teamId: number, rooms: ChatRoom[]) => void;
  openRoom: (room: { id: number; name: string; teamId: number }) => void;
  addMessage: (roomId: number, message: ChatMessage) => void;
  setMessagesForRoom: (roomId: number, messages: ChatMessage[]) => void;
}

// Store 생성
export const chatStore = new Store<ChatStoreState>({
  // 초기값...
  expandedTeams: new Set(),
  roomsByTeam: new Map(),
  activeRoomId: null,
  messagesByRoom: new Map(),

  toggleTeamExpand: (teamId: number) => {
    chatStore.setState((state) => {
      const newExpanded = new Set(state.expandedTeams);
      if (newExpanded.has(teamId)) {
        newExpanded.delete(teamId);
      } else {
        newExpanded.add(teamId);
      }
      return { ...state, expandedTeams: newExpanded };
    });
  },

  setRoomsForTeam: (teamId: number, rooms: ChatRoom[]) => {
    chatStore.setState((state) => {
      const newMap = new Map(state.roomsByTeam);
      newMap.set(teamId, rooms);
      return { ...state, roomsByTeam: newMap };
    });
  },

  openRoom: (room) => {
    chatStore.setState((state) => {
      // 탭 시스템과 연동 - roomId를 탭으로 열기
      // 기존 openTab과 유사하지만 타입 구분 필요
      return { ...state, activeRoomId: room.id };
    });
  },

  addMessage: (roomId: number, message: ChatMessage) => {
    chatStore.setState((state) => {
      const newMap = new Map(state.messagesByRoom);
      const existing = newMap.get(roomId) || [];
      newMap.set(roomId, [...existing, message]);
      return { ...state, messagesByRoom: newMap };
    });
  },

  setMessagesForRoom: (roomId: number, messages: ChatMessage[]) => {
    chatStore.setState((state) => {
      const newMap = new Map(state.messagesByRoom);
      newMap.set(roomId, messages);
      return { ...state, messagesByRoom: newMap };
    });
  },
});

// React Hook
export const useChatStore = <T>(selector: (state: ChatStoreState) => T): T => {
  return useStore(chatStore, selector);
};
```

**TanStack Query 연동** (데이터 페칭)

```typescript
// useChatRooms.ts
import { useQuery, useMutation, useQueryClient } from "@tanstack/react-query";
import { chatRoomApi } from "../api/chatRoomApi";

// 팀별 채팅방 목록 조회
export const useRoomsByTeam = (teamId: number) => {
  return useQuery({
    queryKey: ["chat-rooms", "team", teamId],
    queryFn: () => chatRoomApi.getRoomsByTeam(teamId),
    enabled: teamId > 0,
    staleTime: 1000 * 60 * 5,
  });
};

// 채팅방 메시지 조회 (페이징)
export const useRoomMessages = (roomId: number, page: number = 0) => {
  return useQuery({
    queryKey: ["chat-messages", roomId, page],
    queryFn: () => chatRoomApi.getMessages(roomId, page),
    enabled: roomId > 0,
    staleTime: 1000 * 60,
  });
};

// 채팅방 생성
export const useCreateRoom = () => {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: chatRoomApi.createRoom,
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ 
        queryKey: ["chat-rooms", "team", variables.teamId] 
      });
    },
  });
};

// 메시지 전송
export const useSendMessage = () => {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: chatRoomApi.sendMessage,
    onSuccess: (_, variables) => {
      queryClient.invalidateQueries({ 
        queryKey: ["chat-messages", variables.roomId] 
      });
    },
  });
};
```

---

### 1.4 구현 순서

#### Phase 1: 기본 구조 (1일)
1. DB 스키마 추가 (chat_rooms, chat_messages, chat_room_participants)
2. 백엔드 모듈 생성 (chat-rooms)
3. REST API 구현 (CRUD)

#### Phase 2: 프론트엔드 트리 메뉴 (1일)
1. ChatSidebar에 팀 하위 채팅방 표시
2. 채팅방 CRUD UI
3. 탭 시스템 연동

#### Phase 3: 채팅 UI (1일)
1. ChatRoomContent 컴포넌트
2. 메시지 목록 표시
3. 메시지 입력/전송 (REST API로 우선 구현)

#### Phase 4: 실시간 채팅 (1-2일)
1. NestJS WebSocket Gateway 설정
2. Socket.io 또는 WS 연동
3. 실시간 메시지 송수신
4. 참여자 상태 표시

---

## 2차 구현 (AI 기능)

### 2.1 AI 참여 모드

- 채팅방 생성 시 `room_type: AI_ENABLED` 선택 가능
- AI 모드 활성화 시 Gemini/GPT가 대화에 참여
- AI 멘션 기능: `@AI 질문내용` 형식으로 AI에게 질문

### 2.2 AI 응답 로직

```typescript
// AI 모드가 켜진 채팅방에서
if (room.roomType === 'AI_ENABLED' && message.includes('@AI')) {
  const question = message.replace('@AI', '').trim();
  const aiResponse = await geminiService.generateResponse(question);
  broadcastMessage({
    roomId,
    userId: AI_USER_ID,
    userName: 'AI Assistant',
    content: aiResponse,
    messageType: 'AI'
  });
}
```

### 2.3 추가 기능 (향후)

- 대화 요약 기능
- 코드 리뷰 봇
- 번역 기능
- 음성 채팅 (WebRTC)

---

## 파일 구조 (예상)

### 백엔드
```
src/
├── chat-rooms/
│   ├── chat-rooms.module.ts
│   ├── chat-rooms.controller.ts
│   ├── chat-rooms.service.ts
│   ├── chat-rooms.gateway.ts      # WebSocket
│   └── dto/
│       ├── create-chat-room.dto.ts
│       └── update-chat-room.dto.ts
└── db/
    └── schema.ts                   # 테이블 추가
```

### 프론트엔드
```
src/
├── features/
│   └── chat-management/
│       ├── api/
│       │   └── chatRoomApi.ts      # 신규
│       └── model/
│           └── useChatRooms.ts     # 신규
└── widgets/
    ├── chat-sidebar/
    │   └── ui/
    │       ├── ChatSidebar.tsx     # 수정
    │       ├── ChatTeamItem.tsx    # 수정 (하위 채팅방 표시)
    │       └── ChatRoomItem.tsx    # 신규
    └── chat-content/
        └── ui/
            ├── ChatMainContent.tsx # 수정
            └── ChatRoomContent.tsx # 신규 (실제 채팅 UI)
```

---

## 요약

| 단계 | 내용 | 예상 기간 |
|------|------|-----------|
| 1차-1 | DB + REST API | 1일 |
| 1차-2 | 트리 메뉴 UI | 1일 |
| 1차-3 | 채팅 UI | 1일 |
| 1차-4 | 실시간 (WebSocket) | 1-2일 |
| 2차 | AI 참여 기능 | 2-3일 |

**총 예상: 1차 4-5일, 2차 2-3일**
